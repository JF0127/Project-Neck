"""Project-Neck online robot runtime.

Heavy deployment dependencies are imported by the concrete runtime modules, not at package
import time. Use ``python -m runtime`` for the application entry point.
"""
